## 2026-08-04 - [Missing Depth Limit & Loose Timestamps]\n**Vulnerability:** validateForCanonicalization allows unbounded object depth and unvalidated timestamp parsing.\n**Learning:** Recursion limits and regex timestamp validation should be added to prevent DoS attacks through excessive payload depths or time parsing confusions.\n**Prevention:** Added depth tracking in validateForCanonicalization and strict regex validation for ISO8601 UTC timestamps.
